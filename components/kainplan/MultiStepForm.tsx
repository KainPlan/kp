import style from './MultiStepForm.module.scss';

interface MultiStepFormProps {
  forms: React.ReactElement[];
};

const MultiStepForm = ({ forms }: MultiStepFormProps) => {

  return (
    <div className={style.root}>
      <div>
        {
        }
      </div>
    </div>
  );
};

export default MultiStepForm;